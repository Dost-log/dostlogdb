import  React  from "react";

const Somecomponent = (props)=>{
    return(
        <div>
            This is the boiler plate for every component folder
            
        </div>
    )
}

export default Somecomponent;